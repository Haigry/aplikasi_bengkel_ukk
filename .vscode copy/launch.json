{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Tailwind Init",
            "program": "${workspaceFolder}/node_modules/.bin/tailwind",
            "args": [
                "init"
            ],
            "preLaunchTask": "install",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}