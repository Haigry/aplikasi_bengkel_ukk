DATABASE_URL="mysql://username:password@localhost:3306/DatabaseName"
JWT_SECRET=YOUR_JWT_SECRET
NEXTAUTH_SECRET="YOUR_NEXTAUTH_SECRET"
NEXTAUTH_URL="YOUR_NEXTAUTH_URL"
```
DATABASE_URL is the URL of the database where the data will be stored.
JWT secret and NextAuth secret can be generated using the following command:
openssl rand -base64 32
NextAuth secret can be generated using the following command:
openssl rand -hex 64
NextAuth URL is the URL of the website where the application is hosted.
Save the file as .env and fill in the required information.
```